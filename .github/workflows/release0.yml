# Minimal build-only workflow — no PR comments, no conditions.
# Use this to confirm the APK builds before using the full release.yml.
name: Build APK (Simple)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'
          cache: gradle

      - run: chmod +x ./gradlew

      - name: Build Release APK
        run: ./gradlew assembleRelease
        env:
          R34_API_KEY: ${{ secrets.R34_API_KEY }}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: breadboard-release
          path: app/build/outputs/apk/release/*.apk
